class Solution:
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        if x < 0:
            return False
        else:
            strx = str(x)
            result = int(strx[::-1])
            if result == x:
                return True
            else:
                return False
